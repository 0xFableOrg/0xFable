{
  // Source: https://github.com/Synthetixio/synpress/blob/dev/tests/e2e/tsconfig.json
  // As explained in README of that repo
  // Needed to add "node" to types for it to compile

  "compilerOptions": {
    "allowJs": true,
    "baseUrl": "../../node_modules",
    "types": [
      "node",
      "cypress",
      "@synthetixio/synpress/support",
      "cypress-wait-until",
      "@testing-library/cypress"
    ],
    "outDir": "./output"
  },
  "include": ["**/*.*"]
}