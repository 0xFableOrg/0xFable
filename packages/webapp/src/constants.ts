/**
 * This module contains project-wide constant values.
 *
 * @module constants
 */

import { localhost } from "wagmi/chains"

/** WalletConnect cloud project ID. */
export const projectId='8934622f70e11b51de893ea309871a4c'

/** The chain our app is deployed on (we only support one allowed chain for now). */
export const chain = localhost